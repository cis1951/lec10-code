import SwiftUI
import WebKit

struct WebView: View {
    var url: URL
    
    var body: some View {
        Text("TODO")
            .background(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
